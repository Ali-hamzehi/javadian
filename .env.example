# Javadian Operations Deployment Environment Variables

# If you don't have a domain yet, leave as :80 to serve HTTP directly via Server IP:
DOMAIN=:80

# When your domain (e.g., ops.yourdomain.ir) is pointed to your server IP, uncomment and set:
# DOMAIN=ops.yourdomain.ir
