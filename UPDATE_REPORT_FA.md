# گزارش جامع تحویل نسخه ارتقایافته سامانه عملیات جوادیان
## بازطراحی تجربه کاربری کارمندان و مدیران (Employee vs Manager UX Overhaul)

این گزارش مرجع جامع تغییرات، معماری جدید، اصلاحات فرآیندی و نتایج اعتبارسنجی بسته نهایی سامانه عملیات جوادیان است.

---

## ۱. اهداف و رویکرد طراحی محصول (Product Design Rationale)

در نسخه پایه، تمام کاربران (از کارمند ساده انبار تا مدیر ارشد مالی و مدیرعامل) با واسط کاربری یکسان و متأثر از مفاهیم پیچیده سازمانی (شامل اصطلاحات فنی مانند WorkItem، SoD، دکمه‌های «توپ در زمین»، ماتریس وضعیت سه‌بعدی و تب‌های درهم‌تنیده) مواجه می‌شدند.

در این بازطراحی عمیق و اصولی، تجربه کاربری با تمرکز بر **جداسازی کامل نقش‌ها** و **پاسخ فوری به ۴ سؤال کلیدی کارمند** بازآفرینی شد:
1. **چه کاری باید انجام دهم؟**
2. **قدم بعدی چیست؟**
3. **تا چه زمانی فرصت دارم؟**
4. **کاری که ثبت یا ارسال کردم اکنون دست چه کسی است؟**

---

## ۲. معماری جدید کامپوننت‌ها و جریان داده

### الف) ماژول نقش و صلاحیت‌ها (`src/utils/roleExperience.ts`)
- تابع `determineUserExperienceType`: تشخیص قطعی نوع کاربر بین سه وضعیت:
  - **`employee` (کارمند اجرایی)**: مانند `p-ordinary`, `p-sales`, `p-warehouse`, `p-fin-spec`, `p-field-sales`.
  - **`manager` (مدیر و تصمیم‌گیرنده)**: مانند `p-comm-approver`, `p-fin-dir`, `p-ops-dir`, `p-admin-ops`, `p-master-data`.
  - **`hybrid` (نقش دوگانه / دارای تفویض)**: مانند `p-multi-delegate` (محسن راد - کارشناس دارای تفویض اختیارات جانشینی).
- تابع `getAuthorizedRequestTypes`: پالایش دقیق انواع درخواست‌های قابل ثبت بر اساس `capabilities` کاربر (سفارش فروش، تماس مشتری، درخواست تأمین، حواله/رسید انبار، درخواست پرداخت مالی، ارجاع کار).
- تابع `filterEmployeeRecords`: تفکیک بدون تداخل رکوردهای کارمند به سه دسته:
  - `to_do`: کارهای ارجاع‌شده که اقدام مستقیم کارمند را می‌طلبند.
  - `tracking`: درخواست‌های ثبت‌شده توسط کارمند که در انتظار تأیید یا اقدام دیگران است.
  - `history`: پرونده‌های نهایی‌شده، تحویل‌شده یا بایگانی‌شده.
- تابع `getEmployeeOneLineSummary`: تولید نوار آمار عملیاتی زنده و کوتاه («شما ۲ کار منتظر انجام و ۱ درخواست در حال پیگیری دارید»).

### ب) بازطراحی کامل `InboxView.tsx`
- **نمای اختصاصی کارمند**:
  - حذف کلیه زبانه‌های تصمیم‌گیری مدیریتی، نمودارهای حجیم و مفاهیم اداری زائد.
  - ارائه سه زبانه شفاف: «برای انجام (منتظر اقدام من)»، «برای پیگیری (در انتظار دیگران)» و «سابقه».
  - دکمه برجسته «ثبت درخواست جدید» در هدر صفحه.
- **نمای اختصاصی مدیر**:
  - تب‌های راهبردی: «تصمیم‌های من» (با امکان تأیید/رد/عودت سریع)، «پیگیری تیم و گلوگاه‌ها»، و «کارهای شخصی».
  - امکان سوییچ آنی بین «نمای کارتی (Cards)» و «نمای جدول تجمیعی (Dense Table)».
- **سوئیچر نقش دوگانه (Hybrid Switcher)**:
  - کاربران دارای تفویض (مانند محسن راد) دارای نوار تغییر دیدگاه واضح در بالای صفحه بین «کارهای من (کارشناسی)» و «مدیریت و نظارت (تفویض)» هستند تا هیچ تداخلی میان کارهای شخصی و مسئولیت‌های جانشینی رخ ندهد.
- **پنل جستجو و فیلتر پیشرفته**:
  - جستجوی بلادرنگ با امکان باز و بسته شدن فیلترهای فوریت (دارای تأخیر، رو به اتمام، عادی)، فیلتر دسته‌بندی موضوعی و دکمه پاکسازی فیلترها.

### ج) کارت‌های اختصاصی (`EmployeeWorkCard` و `ManagerDecisionCard`)
- **`EmployeeWorkCard`**: کارتی آرام‌بخش با گرادیان ملایم، پاسخ به ۴ سؤال اساسی، نمایش راهنمای گام بعدی، برچسب فوریت زمانی، و نمایش شفاف مسئول فعلی (به جای برچسب‌های تکراری و مبهم).
- **`ManagerDecisionCard`**: نمایش زمینه تصمیم، مانده اعتباری/مبلغی با کامپوننت `CurrencyAmount`، تشخیص گلوگاه و کلیدهای اقدام فوری تأیید، عودت و رد.

### د) بازطراحی پنجره جزئیات (`WorkItemDetailDrawer.tsx`)
- تغییر سلسله‌مراتب بصری: پاسخ مستقیم به «چه کاری باید انجام دهم؟» در اولین کادر بالای دراور با زمینه متمایز.
- بخش «قدم بعدی چیست؟» با راهنمای شفاف اقدام.
- بخش اطلاعات زمانی و مسئول فعلی پرونده.
- نمایش اقلام، مبالغ و اسناد با فرمت استاندارد ریال/تومان و پلاک استاندارد ایرانی.
- پاکسازی کامل کادرهای گیج‌کننده «ابعاد سه‌گانه وضعیت» و اصطلاحات نامأنوس لاتین.

### ه) مودال جامع ثبت درخواست (`SubmitRequestModal.tsx`)
- نمایش دسته‌بندی‌شده فقط گزینه‌هایی که کاربر مجاز به ثبت آنهاست.
- در صورت عدم داشتن مجوز، پیام محترمانه و شفاف نمایش داده می‌شود.
- اتصال به روت‌ها و مودال‌های تخصصی سامانه بدون ایجاد شکست در روت‌های موجود.

---

## ۳. پاکسازی اصطلاحات فنی و نامأنوس (Jargon Elimination)

تمامی اصطلاحات گیج‌کننده شناسایی و با معادل‌های روان و شفاف فارسی جایگزین شدند:
- **SoD (Segregation of Duties)**: جایگزین با «منع خودتأییدی: ثبت‌کننده نمی‌تواند درخواست خودش را تأیید کند».
- **WorkItem / APPROVAL_REVIEW**: جایگزین با عناوین واقعی کار (مانند «تأیید سفارش فروش»، «بررسی درخواست پرداخت»، «صدور حواله خروج»).
- **توپ در زمین**: جایگزین با عنوان رسمی و واضح «مسئول فعلی: [نام شخص یا واحد]».
- **Three Status Dimensions (Lifecycle, Workflow, State)**: حذف کادرهای زائد فنی و نمایش یک برچسب وضعیت شفاف و قابل فهم برای کاربر.

---

## ۴. جدول ارزیابی سناریوهای ۱۱ گانه (Verification Scenarios Matrix)

| شماره | عنوان سناریو | شرح آزمون | وضعیت | توضیحات و شواهد فنی |
| :---: | :--- | :--- | :---: | :--- |
| **۱** | **کارمند ساده (Ordinary Employee)** | ورود با `p-ordinary` یا `p-sales`، عدم مشاهده تب‌های مدیریتی، مشاهده «کارهای من» و پاسخ فوری به «چه کاری باید انجام دهم؟» در کارت و دراور | **PASS** | در تست‌های خودکار و رندرینگ تایید شد؛ تب‌های تصمیم‌گیری مخفی و دراور با بلوک چه کاری باید انجام دهم شروع می‌شود. |
| **۲** | **تفکیک برای انجام و برای پیگیری** | تفکیک وظایف محوله به کارمند از درخواست‌هایی که خودش ثبت کرده و منتظر اقدام دیگران است | **PASS** | توسط `filterEmployeeRecords` تست شد؛ رکوردهای ثبت‌شده با مسئول غیر از خود به تب «برای پیگیری» منتقل می‌شوند. |
| **۳** | **دکمه ثبت درخواست جدید** | باز شدن مودال، نمایش تنها گزینه‌های مجاز برای کاربر، باز شدن فرم مناسب | **PASS** | کامپوننت `SubmitRequestModal` تست شد؛ `p-sales` فقط گزینه‌های فروش و مشتری را می‌بیند. |
| **۴** | **عدم دسترسی به ثبت** | رفتار مناسب سیستم برای کاربری که هیچ مجوز ثبتی ندارد (`p-ordinary` یا `p-no-access`) | **PASS** | در مودال دکمه غیرفعال شده و هشدار محترمانه عدم داشتن مجوز نمایش می‌یابد. |
| **۵** | **حذف اصطلاحات نامأنوس** | نبود عبارات SoD, WorkItem, توپ در زمین و Three Status Dimensions در کل واسط کاربری | **PASS** | با تست رگرسیون متنی `everyday UI eliminates jargon` تایید شد؛ تمام عبارات با معادل فارسی کاربردی جایگزین شدند. |
| **۶** | **تجربه مدیر (Manager UX)** | مشاهده تب‌های «تصمیم‌های من» و «پیگیری تیم»، اقدام مستقیم تأیید/رد/عودت و کشف گلوگاه‌ها برای `p-comm-approver` و `p-fin-dir` | **PASS** | کارت‌های مدیریتی `ManagerDecisionCard` با دکمه‌های اقدام سریع و نمایش مبالغ ریالی فعال هستند. |
| **۷** | **نقش دوگانه (Hybrid Persona)** | امکان سوئیچ روان بین «کارهای من» و «مدیریت و نظارت» برای `p-multi-delegate` (محسن راد) | **PASS** | سوئیچر در هدر صفحه کارتابل برای نقش‌های هیبرید تعبیه شده و به درستی بین دو فیلتر سوییچ می‌کند. |
| **۸** | **نمای جدولی فشرده دسکتاپ** | امکان سوییچ به نمای جدولی متراکم برای مدیران با ترافیک کاری بالا | **PASS** | کلید سوییچ کارت/جدول در نمای مدیریتی پیاده‌سازی شده و جدول واکنش‌گرا با تمام مشخصات رندر می‌شود. |
| **۹** | **واکنش‌گرایی موبایل (۳۹۰px)** | انطباق کامل المان‌ها در ۳۹۰ پیکسل، پیمایش پایین صفحه، ابعاد لمسی دکمه‌ها (حداقل ۴۴px) | **PASS** | تمامی استایل‌ها با کلاس‌های استاندارد Tailwind (`p-4`, `min-h-[44px]`, `w-full sm:w-auto`, flex-col) تطبیق داده شدند. |
| **۱۰** | **عدم شکست تست‌های موجود** | اجرای کامل تمام ۲۴ تست پایه قبلی + تست‌های جدید در `npm test` | **PASS** | تمام ۳۰ تست (۲۴ تست قبلی + ۶ تست سوئیت جدید) با خروج کد ۰ و با موفقیت ۱۰۰٪ پاس شدند. |
| **۱۱** | **حفظ فایل‌های محافظت‌شده** | انطباق بایت‌به‌بایت ۳۱ فایل محافظت‌شده با مقادیر هش SHA-256 | **PASS** | تست `regression.test.tsx` و اسکریپت هش تایید کردند که هیچ‌یک از ۳۱ فایل تغییر نکرده‌اند. |

---

## ۵. نتایج آزمون‌های فنی و بیلد (Build & Test Results)

1. **بررسی انواع تایپ‌اسکریپت (`npm run typecheck`)**:
   - وضعیت: **PASS** (بدون هیچ‌گونه خطا یا هشدار).
2. **سوئیت تست‌های خودکار (`npm test`)**:
   - مجموع تست‌ها: **۳۰ تست** (شامل ۲۴ تست پایه رگرسیون، رابط کاربری و ورک‌فلو + ۶ تست جامع تجربه کارمند).
   - خروجی: **۳۰ Passed, 0 Failed, 0 Skipped**.
3. **تولید بسته نهایی (`npm run build`)**:
   - زمان ساخت: ۳.۸۹ ثانیه در محیط Vite ۶.
   - وضعیت بهینه‌سازی چانک‌ها:
     - تمامی چانک‌های جاوااسکریپت زیر سقف ۵۰۰ کیلوبایت قرار دارند.
     - بزرگ‌ترین چانک: `dist/assets/view-InboxView-hDuoTDBE.js` به حجم **459.96 kB** (۹۸.۶۶ kB به‌صورت فشرده Gzip).
4. **تأیید یکپارچگی فایل‌های محافظت‌شده**:
   - تطابق ۱۰۰٪ مقادیر هش تمامی ۳۱ فایل ذکرشده در `docs/PROTECTED_SOURCE_SHA256.json`.
   - دست نخوردن فایل‌های `src/types/index.ts`, `src/utils/formatters.ts`, و کلیه فایل‌های `src/data/*`.

---

## ۶. محدودیت‌های باقیمانده و گزارش‌دهی صادقانه (Limitations & Transparency)

در چارچوب اخلاق مهندسی و تعهد به شفافیت کامل با کارفرما:
1. **عدم بک‌اند دائمی (In-Memory Prototype)**:
   - این سیستم یک پروتوتایپ کامل کلاینت‌ساید با داده‌های Mock و React 19 است. با بستن تب مرورگر یا رفرش سخت‌افزاری صفحه، وضعیت رکوردهای جدید تغییریافته به مقادیر اولیه بازنشانی می‌شود. برای محیط عملیاتی واقعی نیازمند اتصال به API سرور و پایگاه داده است.
2. **محدودیت ضبط خودکار اسکرین‌شات مرورگر (Headless Browser Limitation)**:
   - در زمان اجرای ساب‌ایجنت مرورگر برای ثبت اسکرین‌شات از ابعاد ۳۹۰، ۷۶۸ و ۱۴۴۰ پیکسل، به دلیل تحریم یا خطای ۴۰۴ سرور منبع Playwright (`https://playwright.azureedge.net/builds/driver/playwright-1.57.0-win32_x64.zip`)، راه‌اندازی مرورگر هِدلس در محیط کانتینر ممکن نگردید.
   - هیچ تصویر فیک یا ساختگی تولید نشد؛ سند `screenshots/README.md` با مستندسازی دقیق این محدودیت به‌روزرسانی شده و پروژه پس از اجرای لوکال در دسترس کامل دیداری قرار دارد.
3. **تأییدیه احکام و مجوزهای امنیتی**:
   - کنترل دسترسی‌های فعلی در سطح لایه نمایش فرانت‌اند صورت می‌گیرد؛ در استقرار نهایی باید یک گیت‌وی احراز هویت و مجوزدهی متمرکز سمت سرور (RBAC/ABAC) مکمل آن باشد.

---

## ۷. راهنمای راه‌اندازی و اجرای محلی (Local Run Instructions)

برای اجرای محلی پروژه روی سیستم خود:

```bash
# ۱. نصب وابستگی‌ها
npm install

# ۲. اجرای تست‌های خودکار (۳۰ تست)
npm test

# ۳. بررسی صحت تایپ‌ها
npm run typecheck

# ۴. اجرای سرور توسعه محلی
npm run dev
```

سامانه بر روی آدرس زیر در دسترس خواهد بود:
**`http://localhost:3000/`**

---

## ۸. بسته نهایی تحویل

فایل فشرده حاوی کل سورس‌کد به‌روزشده (بدون پوشه‌های `node_modules`, `dist`, `.git`) در ریشه پروژه با نام زیر بسته‌بندی و تحویل گردید:
📦 **`javadian-employee-ux-updated.zip`**
